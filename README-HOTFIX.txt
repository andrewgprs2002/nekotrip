NekoTrip v0.5.1.1 Wishlist hotfix

Copy the CONTENTS of this folder directly over your NekoTrip project root.
This fixes the incomplete v0.5.1 update patch by adding the missing route,
repository, and domain types together with the matching wishlist/map UI files.

No npm install is required.
No new SQL migration is required if 0010 and 0011 were already run.
Restart Next.js after copying:
  Ctrl+C
  npm.cmd run dev
Then open:
  http://localhost:3000/wishlist
